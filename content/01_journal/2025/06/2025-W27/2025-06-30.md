---
tags:
  - dailyjournal
journal: 聖護院のUFJまでチャリで行って、手続をした。
weather: 3
mood: 7
sleep: 7
exercise: 105
reading/study: 0
weight: 68
FatRate: 16.8
run: true
reading: true
study: 
stretch: true
game:
---
p`button-excali` `button-Thino` `button-news`

> [!Summary]+ ☀️DailyConditions☀️
> ```dataviewjs
> // Templaterで埋め込まれる日付を起点にする（例: 2025-06-23）
>const today = moment("2025-06-30", "YYYY-MM-DD").startOf("day");
>const sevenDaysAgo = today.clone().subtract(6, "days"); // 7日間分（今日含む）
>
>const pages = dv.pages('#dailyjournal')
>  .filter(p => {
>	const date = moment(p.file.name, "YYYY-MM-DD");
>	return date.isBetween(sevenDaysAgo, today.clone().add(1, "day"), null, '[)');
>  })
>  .filter(p => p.file.name !== "temp_daily_journal")
>  .sort(p => p.file.name);
>
>function extract(pages, key) {
>  return pages.map(p => p[key]).values;
>}
>
>const days = pages.map(p => p.file.name).values;
>const mood = extract(pages, 'mood');
>const weight = extract(pages, 'weight');
>const sleep = extract(pages, 'sleep');
>const weather = extract(pages, 'weather');
>
>function totalChart(mood, sleep, weather) {
>  return {
>	type: 'line',
>	data: {
>	  labels: days,
>	  datasets: [{
>		label: 'sleep',
>		data: sleep,
>		borderColor: ['rgba(255, 99, 132, 1)'],
>		backgroundColor: ['rgba(255, 99, 132, 0.7)']
>	  },{
>		label: 'weather',
>		data: weather,
>		borderColor: ['rgba(235, 113, 2, 1)'],
>		backgroundColor: ['rgba(235, 113, 2, 1)']
>	  },{
>		label: 'mood',
>		data: mood,
>		type: 'bar',
>		backgroundColor: ['rgba(75, 148, 78, 1.0)'],  
>		borderColor: ['rgba(99, 255, 132, 1)']
>	  }]
>	}
> };
>}
>
>const totalscore = totalChart(mood, sleep, weather);
>this.container.style.width = "80%";   // 横幅
>this.container.style.height = "60%";  // 高さ
>window.renderChart(totalscore, this.container);

> [!todo]+ ✒️TODAY's LINE UP✒️
>```tasks 
>happens "2025-06-30" 
>filter by function ! 'NON_TASK,CANCELLED,DONE'.includes(task.status.type)
>sort by description
>short mode
>```

>[!danger]+ 🔥💣OVERDUE💣🔥
>```tasks
>happens before today
>filter by function ! task.isDone
>filter by function ! 'NON_TASK,CANCELLED'.includes(task.status.type)
>sort by description 
>short mode
>```


# 🗒️NOTES CLIPPED🗒️
```dataviewjs
// ファイル名から日付を取得（例: "2025-05-07"）
const fileDateStr = dv.current().file.name;
const targetDate = moment(fileDateStr, "YYYY-MM-DD").startOf("day");
const nextDate = moment(targetDate).add(1, "day");

dv.list(
  dv.pages('"Clippings"')
    .where(p => p.file.ctime >= targetDate && p.file.ctime < nextDate)
    .sort(p => p.file.ctime, 'asc')
    .map(p => `${p.description ?? ""} ![image](${p.image ?? ""})`)
);
```

# 📅Day Planner📅


# 🚣TIMELINE🚣
```dataviewjs
const fileDateStr = dv.current().file.name;
const targetDate = moment(fileDateStr, "YYYY-MM-DD").startOf("day");
const nextDate = moment(targetDate).add(1, "day");

const imageExtensions = [".png", ".jpg", ".jpeg", ".webp", ".gif", ".bmp"];
dv.table(["ファイル", "作成日時"],
  dv.pages()
    .where(p =>
      p.file.ctime >= targetDate &&
      p.file.ctime < nextDate &&
      !p.file.path.includes("Clippings") &&
      !imageExtensions.some(ext => p.file.name.toLowerCase().endsWith(ext))
    )
    .sort(p => p.file.ctime, 'asc')
    .map(p => [
      p.file.link,
      `${p.file.ctime.toFormat("yyyy年M月d日 H時mm分")}`
    ])
);
```
- [x] 08:30-09:00 移動 #exercise/bicycle ;30 ✅ 2025-06-30
- 09:17 チャリで聖護院のUFjにきた。
- [x] 09:15-10:15 UFJ銀行で手続き #forwowrk/else ;60 ✅ 2025-06-30
- [x] 10:30-11:00 移動  #exercise/bicycle ;30 ✅ 2025-06-30
- [x] 11:00-11:15 経費口座開設手続き #forwork/else ✅ 2025-06-30
- [x]  ✅ 2025-07-13