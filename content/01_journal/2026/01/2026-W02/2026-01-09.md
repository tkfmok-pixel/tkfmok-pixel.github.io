---
tags:
  - dailyjournal
journal: 1週間生き延びた
mood: 5
weather: 3
EarlyUp:
study:
exercise:
newspaper: true
SleepFrom: 2026-01-08T23:33:00
SleepTo: 2026-01-09T07:28:00
SleepScore: 84
weight:
FatRate:
Calorie: 1919
WorkTime: 8
ExerciseTime: 0
RunningDistance: 0
Steps: 2008
StudyTime:
---


# ✒️TODAY's LINE UP✒️
```tasks 
happens "2026-01-09" 
filter by function ! 'NON_TASK,CANCELLED,DONE'.includes(task.status.type)
sort by description
short mode
```

# 🔥💣OVERDUE💣🔥
```tasks
happens before today
filter by function ! task.isDone
filter by function ! 'NON_TASK,CANCELLED'.includes(task.status.type)
sort by description 
short mode
```

# 🗒️NOTES CLIPPED🗒️
```dataviewjs
const fileDateStr = dv.current().file.name;
const targetDate = moment(fileDateStr, "YYYY-MM-DD").startOf("day");
const nextDate = moment(targetDate).add(1, "day");

dv.list(
  dv.pages('"Clippings"')
    .where(p => p.file.ctime >= targetDate && p.file.ctime < nextDate)
    .sort(p => p.file.ctime, 'asc')
    .map(p => {
      const linkTitle = `[[${p.file.name}]]：`;
      return `${linkTitle}${p.description ?? ""}<br><br><img src="${p.image ?? ""}" width="400px" height="200px">`;
    })
);
```
# ✒️NOTES CREATED✒️

```dataviewjs
const fileDateStr = dv.current().file.name;
const targetDate = moment(fileDateStr, "YYYY-MM-DD").startOf("day");
const nextDate = moment(targetDate).add(1, "day");

const imageExtensions = [".png", ".jpg", ".jpeg", ".webp", ".gif", ".bmp"];
dv.table(["ファイル", "作成日時"],
  dv.pages()
    .where(p =>
      p.file.ctime >= targetDate &&
      p.file.ctime < nextDate &&
      !p.file.path.includes("Clippings") &&
      !imageExtensions.some(ext => p.file.name.toLowerCase().endsWith(ext))
    )
    .sort(p => p.file.ctime, 'asc')
    .map(p => [
      p.file.link,
      `${p.file.ctime.toFormat("yyyy年M月d日 H時mm分")}`
    ])
);
```

# 📅Day Planner📅
- [x] ライフログ #remind-at-due ⏰ 20:00 📅 2026-01-09 ✅ 2026-01-09

# 🚣TIMELINE🚣

# 🧘 Mindfulness Log [夜]

**Data Fields:** 
mental_score:: 7 
energy_level:: 6 
primary_emotion:: "Relief" 
tags:: #mindfulness #evening 
counselor_insight:: "不安という未来への投影に対し、食事の多幸感という『今ここ』の感覚で適切にバランスを取れています。"

#### 💭 Reflection Summary

仕事の停滞による [[破滅的思考]] から生じる身体の緊張を、進捗という成功体験による「身体の軽さ」で打ち消した一日でした。脳が予測する「怒られる」という幻想よりも、実際に感じた「布団の心地よさ」や「食事の味」といった [[感覚入力]] を優先して観察できている点は、データ的にも非常に健全な回復傾向を示しています。

自身の [[責任感]] を認めつつ、週末は「今この瞬間」の快に身を委ねることで、心身の [[リソースの再構築]] が進むでしょう。