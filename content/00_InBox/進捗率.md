# 進捗率
``` dataviewjs
// 1. ノートの「生のテキストデータ」をすべて読み込む
// Dataviewの解釈を通さず、書かれている文字そのものを取得します
const content = await dv.io.load(dv.current().file.path);

// 2. 正規表現でチェックボックスを探す
// パターン解説: 
// ^ ... 行の先頭から
// \s* ... スペースがあってもOK
// (?:[-*+]|\d+\.) ... 「-」か「*」か「数字.」のどれかがある（箇条書きor番号付き）
// \s* ... スペース
// \[(.|x|X)\] ... [ ] か [x] か [X] がある
// gm ... 全行に対して実行
const regex = /^\s*(?:[-*+]|\d+\.)\s*\[(\s|x|X)\]/gm;

let total = 0;
let completed = 0;

// マッチした行をすべてチェック
const matches = content.match(regex);

if (matches) {